---
import Hero from '../components/Hero.astro';
import BaseLayout from '../layouts/BaseLayout.astro';
import { getLanguageFromURL, defaultLang } from '../i18n/utils';

// Redirect to preferred language
const url = Astro.url;
const lang = getLanguageFromURL(url) || defaultLang;
---

<meta http-equiv="refresh" content={`0;url=/${lang}/404`} />

<BaseLayout title="Not Found" description="404 Error — this page was not found">
	<Hero title="Page Not Found" tagline="Not found" />
</BaseLayout>
